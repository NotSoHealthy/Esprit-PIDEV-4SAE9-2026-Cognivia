package com.pidev.monitoring.entities;

/**
 * Mirrors the Severity enum in the care microservice.
 * Used to target general assignments at a specific patient severity group.
 */
public enum SeverityTarget {
    LOW,
    MEDIUM,
    HIGH,
    EXTREME
}
