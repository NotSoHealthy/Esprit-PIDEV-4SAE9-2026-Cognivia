apiVersion: v1
kind: ConfigMap
metadata:
  name: service-links
  namespace: pidev-deployment
data:
  EUREKA_LINK: "http://eureka:8761/eureka/"
  # Must be reachable from inside the cluster; Ingress for auth.local listens on port 80
  KEYCLOAK_LINK: "http://auth.local/realms/pidev"